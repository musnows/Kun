## 1. Shared Provider Identity

- [x] 1.1 Add validated preset-source metadata to provider profile contracts and populate it from preset factories with legacy exact-id inference.
- [x] 1.2 Implement deterministic multi-account profile allocation with canonical first identities, monotonic suffixes, and global id/name collision avoidance.
- [x] 1.3 Resolve preset model/capability backfill, API-key rules, regional endpoints, and MiniMax media defaults through preset-source metadata.

## 2. Provider Settings Experience

- [x] 2.1 Change repeated subscription and Token Plan selections to create independent drafts while preserving pay-as-you-go preset refresh behavior.
- [x] 2.2 Keep duplicated preset accounts grouped, badged, identity-locked, and independently visible in provider/model/route selectors.
- [x] 2.3 Show localized configured-account counts and add-account messaging in the provider creation dialog.

## 3. Verification And Delivery

- [x] 3.1 Add shared tests for source normalization, legacy compatibility, numbering/collisions, capability retention, and independent runtime credentials.
- [x] 3.2 Add renderer interaction tests for repeated plan creation, account count display, draft cancellation/save, and API preset update preservation.
- [x] 3.3 Run focused tests, typecheck, lint, `build:kun`, top-level build, and diff/OpenSpec validation.
